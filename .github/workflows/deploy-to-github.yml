name: Deploy to GitHub Packages
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: temurin
          cache: maven
          
      - name: Publish Package - lib-hl7v2-nist
        run: mvn -B package

      - name: Publish package to github Repo
        run: mvn -DskipTests clean deploy --settings .github/workflows/mvn-settings.xml
        env:
          LIB_GITHUB_ACTOR: '${{ secrets.LIB_GITHUB_ACTOR }}'
          LIB_GITHUB_TOKEN: '${{ secrets.LIB_GITHUB_TOKEN }}'